/*
 * Circle Messages
 *
 * This contains all the text for the Circle container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Circle';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Circle container!',
  },
});
